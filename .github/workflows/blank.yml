name: CI

on:
  push:
  pull_request:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: set output with warning
        shell: pwsh
        run: |
          echo "::set-output name=VERSION::V1"
